# Deploy test

Marcador para testar se o deploy do EasyPanel funciona neste projeto (mmcriativos).

- 2026-06-14: teste de deploy via UI do EasyPanel para isolar se o bug de download
  (`curl | tar` abortando ~1361 bytes) é específico do app `mmcc/mmcriativos-cloud`
  (repo de 240MB) ou global do EasyPanel.
